import 'package:flutter/material.dart';

class Butonlar extends StatelessWidget {
  const Butonlar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Buton Ekranı"),
      ),
      body: Center(),
    );
  }
}
